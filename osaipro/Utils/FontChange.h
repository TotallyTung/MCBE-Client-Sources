#include <regex>
class FontChange {
public:
	static std::string circle(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"Ⓐ");
		text = std::regex_replace(text, (std::regex)"B", u8"Ⓑ");
		text = std::regex_replace(text, (std::regex)"C", u8"Ⓒ");
		text = std::regex_replace(text, (std::regex)"D", u8"Ⓓ");
		text = std::regex_replace(text, (std::regex)"E", u8"Ⓔ");
		text = std::regex_replace(text, (std::regex)"F", u8"Ⓕ");
		text = std::regex_replace(text, (std::regex)"G", u8"Ⓖ");
		text = std::regex_replace(text, (std::regex)"H", u8"Ⓗ");
		text = std::regex_replace(text, (std::regex)"I", u8"Ⓘ");
		text = std::regex_replace(text, (std::regex)"J", u8"Ⓙ");
		text = std::regex_replace(text, (std::regex)"K", u8"Ⓚ");
		text = std::regex_replace(text, (std::regex)"L", u8"Ⓛ");
		text = std::regex_replace(text, (std::regex)"M", u8"Ⓜ");
		text = std::regex_replace(text, (std::regex)"N", u8"Ⓝ");
		text = std::regex_replace(text, (std::regex)"O", u8"Ⓞ");
		text = std::regex_replace(text, (std::regex)"P", u8"Ⓟ");
		text = std::regex_replace(text, (std::regex)"Q", u8"Ⓠ");
		text = std::regex_replace(text, (std::regex)"R", u8"Ⓡ");
		text = std::regex_replace(text, (std::regex)"S", u8"Ⓢ");
		text = std::regex_replace(text, (std::regex)"T", u8"Ⓣ");
		text = std::regex_replace(text, (std::regex)"U", u8"Ⓤ");
		text = std::regex_replace(text, (std::regex)"V", u8"Ⓥ");
		text = std::regex_replace(text, (std::regex)"W", u8"Ⓦ");
		text = std::regex_replace(text, (std::regex)"X", u8"Ⓧ");
		text = std::regex_replace(text, (std::regex)"Y", u8"Ⓨ");
		text = std::regex_replace(text, (std::regex)"Z", u8"Ⓩ");

		text = std::regex_replace(text, (std::regex)"a", u8"ⓐ");
		text = std::regex_replace(text, (std::regex)"b", u8"ⓑ");
		text = std::regex_replace(text, (std::regex)"c", u8"ⓒ");
		text = std::regex_replace(text, (std::regex)"d", u8"ⓓ");
		text = std::regex_replace(text, (std::regex)"e", u8"ⓔ");
		text = std::regex_replace(text, (std::regex)"f", u8"ⓕ");
		text = std::regex_replace(text, (std::regex)"g", u8"ⓖ");
		text = std::regex_replace(text, (std::regex)"h", u8"ⓗ");
		text = std::regex_replace(text, (std::regex)"i", u8"ⓘ");
		text = std::regex_replace(text, (std::regex)"j", u8"ⓙ");
		text = std::regex_replace(text, (std::regex)"k", u8"ⓚ");
		text = std::regex_replace(text, (std::regex)"l", u8"ⓛ");
		text = std::regex_replace(text, (std::regex)"m", u8"ⓜ");
		text = std::regex_replace(text, (std::regex)"n", u8"ⓝ");
		text = std::regex_replace(text, (std::regex)"o", u8"ⓞ");
		text = std::regex_replace(text, (std::regex)"p", u8"ⓟ");
		text = std::regex_replace(text, (std::regex)"q", u8"ⓠ");
		text = std::regex_replace(text, (std::regex)"r", u8"ⓡ");
		text = std::regex_replace(text, (std::regex)"s", u8"ⓢ");
		text = std::regex_replace(text, (std::regex)"t", u8"ⓣ");
		text = std::regex_replace(text, (std::regex)"u", u8"ⓤ");
		text = std::regex_replace(text, (std::regex)"v", u8"ⓥ");
		text = std::regex_replace(text, (std::regex)"w", u8"ⓦ");
		text = std::regex_replace(text, (std::regex)"x", u8"ⓧ");
		text = std::regex_replace(text, (std::regex)"y", u8"ⓨ");
		text = std::regex_replace(text, (std::regex)"z", u8"ⓩ");

		text = std::regex_replace(text, (std::regex)"1", u8"①");
		text = std::regex_replace(text, (std::regex)"2", u8"②");
		text = std::regex_replace(text, (std::regex)"3", u8"③");
		text = std::regex_replace(text, (std::regex)"4", u8"④");
		text = std::regex_replace(text, (std::regex)"5", u8"⑤");
		text = std::regex_replace(text, (std::regex)"6", u8"⑥");
		text = std::regex_replace(text, (std::regex)"7", u8"⑦");
		text = std::regex_replace(text, (std::regex)"8", u8"⑧");
		text = std::regex_replace(text, (std::regex)"9", u8"⑨");
		text = std::regex_replace(text, (std::regex)"0", u8"⓪");

		return text;
	};
	static std::string allcaps(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"Ａ");
		text = std::regex_replace(text, (std::regex)"B", u8"Ｂ");
		text = std::regex_replace(text, (std::regex)"C", u8"Ｃ");
		text = std::regex_replace(text, (std::regex)"D", u8"Ｄ");
		text = std::regex_replace(text, (std::regex)"E", u8"Ｅ");
		text = std::regex_replace(text, (std::regex)"F", u8"Ｆ");
		text = std::regex_replace(text, (std::regex)"G", u8"Ｇ");
		text = std::regex_replace(text, (std::regex)"H", u8"Ｈ");
		text = std::regex_replace(text, (std::regex)"I", u8"Ｉ");
		text = std::regex_replace(text, (std::regex)"J", u8"Ｊ");
		text = std::regex_replace(text, (std::regex)"K", u8"Ｋ");
		text = std::regex_replace(text, (std::regex)"L", u8"Ｌ");
		text = std::regex_replace(text, (std::regex)"M", u8"Ｍ");
		text = std::regex_replace(text, (std::regex)"N", u8"Ｎ");
		text = std::regex_replace(text, (std::regex)"O", u8"Ｏ");
		text = std::regex_replace(text, (std::regex)"P", u8"Ｐ");
		text = std::regex_replace(text, (std::regex)"Q", u8"Ｑ");
		text = std::regex_replace(text, (std::regex)"R", u8"Ｒ");
		text = std::regex_replace(text, (std::regex)"S", u8"Ｓ");
		text = std::regex_replace(text, (std::regex)"T", u8"Ｔ");
		text = std::regex_replace(text, (std::regex)"U", u8"Ｕ");
		text = std::regex_replace(text, (std::regex)"V", u8"Ｖ");
		text = std::regex_replace(text, (std::regex)"W", u8"Ｗ");
		text = std::regex_replace(text, (std::regex)"X", u8"Ｘ");
		text = std::regex_replace(text, (std::regex)"Y", u8"Ｙ");
		text = std::regex_replace(text, (std::regex)"Z", u8"Ｚ");

		text = std::regex_replace(text, (std::regex)"a", u8"ａ");
		text = std::regex_replace(text, (std::regex)"b", u8"ｂ");
		text = std::regex_replace(text, (std::regex)"c", u8"ｃ");
		text = std::regex_replace(text, (std::regex)"d", u8"ｄ");
		text = std::regex_replace(text, (std::regex)"e", u8"ｅ");
		text = std::regex_replace(text, (std::regex)"f", u8"ｆ");
		text = std::regex_replace(text, (std::regex)"g", u8"ｇ");
		text = std::regex_replace(text, (std::regex)"h", u8"ｈ");
		text = std::regex_replace(text, (std::regex)"i", u8"ｉ");
		text = std::regex_replace(text, (std::regex)"j", u8"ｊ");
		text = std::regex_replace(text, (std::regex)"k", u8"ｋ");
		text = std::regex_replace(text, (std::regex)"l", u8"ｌ");
		text = std::regex_replace(text, (std::regex)"m", u8"ｍ");
		text = std::regex_replace(text, (std::regex)"n", u8"ｎ");
		text = std::regex_replace(text, (std::regex)"o", u8"ｏ");
		text = std::regex_replace(text, (std::regex)"p", u8"ｐ");
		text = std::regex_replace(text, (std::regex)"q", u8"ｑ");
		text = std::regex_replace(text, (std::regex)"r", u8"ｒ");
		text = std::regex_replace(text, (std::regex)"s", u8"ｓ");
		text = std::regex_replace(text, (std::regex)"t", u8"ｔ");
		text = std::regex_replace(text, (std::regex)"u", u8"ｕ");
		text = std::regex_replace(text, (std::regex)"v", u8"ｖ");
		text = std::regex_replace(text, (std::regex)"w", u8"ｗ");
		text = std::regex_replace(text, (std::regex)"x", u8"ｘ");
		text = std::regex_replace(text, (std::regex)"y", u8"ｙ");
		text = std::regex_replace(text, (std::regex)"z", u8"ｚ");

		text = std::regex_replace(text, (std::regex)"0", u8"０");
		text = std::regex_replace(text, (std::regex)"1", u8"１");
		text = std::regex_replace(text, (std::regex)"2", u8"２");
		text = std::regex_replace(text, (std::regex)"3", u8"３");
		text = std::regex_replace(text, (std::regex)"4", u8"４");
		text = std::regex_replace(text, (std::regex)"5", u8"５");
		text = std::regex_replace(text, (std::regex)"6", u8"６");
		text = std::regex_replace(text, (std::regex)"7", u8"７");
		text = std::regex_replace(text, (std::regex)"8", u8"８");
		text = std::regex_replace(text, (std::regex)"9", u8"９");
		return text;
	};
	static std::string landing(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"𝔸");
		text = std::regex_replace(text, (std::regex)"B", u8"𝔹");
		text = std::regex_replace(text, (std::regex)"C", u8"ℂ");
		text = std::regex_replace(text, (std::regex)"D", u8"𝔻");
		text = std::regex_replace(text, (std::regex)"E", u8"𝔼");
		text = std::regex_replace(text, (std::regex)"F", u8"𝔽");
		text = std::regex_replace(text, (std::regex)"G", u8"𝔾");
		text = std::regex_replace(text, (std::regex)"H", u8"ℍ");
		text = std::regex_replace(text, (std::regex)"I", u8"𝕀");
		text = std::regex_replace(text, (std::regex)"J", u8"𝕁");
		text = std::regex_replace(text, (std::regex)"K", u8"𝕂");
		text = std::regex_replace(text, (std::regex)"L", u8"𝕃");
		text = std::regex_replace(text, (std::regex)"M", u8"𝕄");
		text = std::regex_replace(text, (std::regex)"N", u8"ℕ");
		text = std::regex_replace(text, (std::regex)"O", u8"𝕆");
		text = std::regex_replace(text, (std::regex)"P", u8"ℙ");
		text = std::regex_replace(text, (std::regex)"Q", u8"ℚ");
		text = std::regex_replace(text, (std::regex)"R", u8"ℝ");
		text = std::regex_replace(text, (std::regex)"S", u8"𝕊");
		text = std::regex_replace(text, (std::regex)"T", u8"𝕋");
		text = std::regex_replace(text, (std::regex)"U", u8"𝕌");
		text = std::regex_replace(text, (std::regex)"V", u8"𝕍");
		text = std::regex_replace(text, (std::regex)"W", u8"𝕎");
		text = std::regex_replace(text, (std::regex)"X", u8"𝕏");
		text = std::regex_replace(text, (std::regex)"Y", u8"𝕐");
		text = std::regex_replace(text, (std::regex)"Z", u8"ℤ");

		text = std::regex_replace(text, (std::regex)"a", u8"𝕒");
		text = std::regex_replace(text, (std::regex)"b", u8"𝕓");
		text = std::regex_replace(text, (std::regex)"c", u8"𝕔");
		text = std::regex_replace(text, (std::regex)"d", u8"𝕕");
		text = std::regex_replace(text, (std::regex)"e", u8"𝕖");
		text = std::regex_replace(text, (std::regex)"f", u8"𝕗");
		text = std::regex_replace(text, (std::regex)"g", u8"𝕘");
		text = std::regex_replace(text, (std::regex)"h", u8"𝕙");
		text = std::regex_replace(text, (std::regex)"i", u8"𝕚");
		text = std::regex_replace(text, (std::regex)"j", u8"𝕛");
		text = std::regex_replace(text, (std::regex)"k", u8"𝕜");
		text = std::regex_replace(text, (std::regex)"l", u8"𝕝");
		text = std::regex_replace(text, (std::regex)"m", u8"𝕞");
		text = std::regex_replace(text, (std::regex)"n", u8"𝕟");
		text = std::regex_replace(text, (std::regex)"o", u8"𝕠");
		text = std::regex_replace(text, (std::regex)"p", u8"𝕡");
		text = std::regex_replace(text, (std::regex)"q", u8"𝕢");
		text = std::regex_replace(text, (std::regex)"r", u8"𝕣");
		text = std::regex_replace(text, (std::regex)"s", u8"𝕤");
		text = std::regex_replace(text, (std::regex)"t", u8"𝕥");
		text = std::regex_replace(text, (std::regex)"u", u8"𝕦");
		text = std::regex_replace(text, (std::regex)"v", u8"𝕧");
		text = std::regex_replace(text, (std::regex)"w", u8"𝕨");
		text = std::regex_replace(text, (std::regex)"x", u8"𝕩");
		text = std::regex_replace(text, (std::regex)"y", u8"𝕪");
		text = std::regex_replace(text, (std::regex)"z", u8"𝕫");

		text = std::regex_replace(text, (std::regex)"0", u8"𝟘");
		text = std::regex_replace(text, (std::regex)"1", u8"𝟙");
		text = std::regex_replace(text, (std::regex)"2", u8"𝟚");
		text = std::regex_replace(text, (std::regex)"3", u8"𝟛");
		text = std::regex_replace(text, (std::regex)"4", u8"𝟜");
		text = std::regex_replace(text, (std::regex)"5", u8"𝟝");
		text = std::regex_replace(text, (std::regex)"6", u8"𝟞");
		text = std::regex_replace(text, (std::regex)"7", u8"𝟟");
		text = std::regex_replace(text, (std::regex)"8", u8"𝟠");
		text = std::regex_replace(text, (std::regex)"9", u8"𝟡");
		return text;
	};
	static std::string BlackPill(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"🅐");
		text = std::regex_replace(text, (std::regex)"B", u8"🅑");
		text = std::regex_replace(text, (std::regex)"C", u8"🅒");
		text = std::regex_replace(text, (std::regex)"D", u8"🅓");
		text = std::regex_replace(text, (std::regex)"E", u8"🅔");
		text = std::regex_replace(text, (std::regex)"F", u8"🅕");
		text = std::regex_replace(text, (std::regex)"G", u8"🅖");
		text = std::regex_replace(text, (std::regex)"H", u8"🅗");
		text = std::regex_replace(text, (std::regex)"I", u8"🅘");
		text = std::regex_replace(text, (std::regex)"J", u8"🅙");
		text = std::regex_replace(text, (std::regex)"K", u8"🅚");
		text = std::regex_replace(text, (std::regex)"L", u8"🅛");
		text = std::regex_replace(text, (std::regex)"M", u8"🅜");
		text = std::regex_replace(text, (std::regex)"N", u8"🅝");
		text = std::regex_replace(text, (std::regex)"O", u8"🅞");
		text = std::regex_replace(text, (std::regex)"P", u8"🅟");
		text = std::regex_replace(text, (std::regex)"Q", u8"🅠");
		text = std::regex_replace(text, (std::regex)"R", u8"🅡");
		text = std::regex_replace(text, (std::regex)"S", u8"🅢");
		text = std::regex_replace(text, (std::regex)"T", u8"🅣");
		text = std::regex_replace(text, (std::regex)"U", u8"🅤");
		text = std::regex_replace(text, (std::regex)"V", u8"🅥");
		text = std::regex_replace(text, (std::regex)"W", u8"🅦");
		text = std::regex_replace(text, (std::regex)"X", u8"🅧");
		text = std::regex_replace(text, (std::regex)"Y", u8"🅨");
		text = std::regex_replace(text, (std::regex)"Z", u8"🅩");

		text = std::regex_replace(text, (std::regex)"a", u8"🅐");
		text = std::regex_replace(text, (std::regex)"b", u8"🅑");
		text = std::regex_replace(text, (std::regex)"c", u8"🅒");
		text = std::regex_replace(text, (std::regex)"d", u8"🅓");
		text = std::regex_replace(text, (std::regex)"e", u8"🅔");
		text = std::regex_replace(text, (std::regex)"f", u8"🅕");
		text = std::regex_replace(text, (std::regex)"g", u8"🅖");
		text = std::regex_replace(text, (std::regex)"h", u8"🅗");
		text = std::regex_replace(text, (std::regex)"i", u8"🅘");
		text = std::regex_replace(text, (std::regex)"j", u8"🅙");
		text = std::regex_replace(text, (std::regex)"k", u8"🅚");
		text = std::regex_replace(text, (std::regex)"l", u8"🅛");
		text = std::regex_replace(text, (std::regex)"m", u8"🅜");
		text = std::regex_replace(text, (std::regex)"n", u8"🅝");
		text = std::regex_replace(text, (std::regex)"o", u8"🅞");
		text = std::regex_replace(text, (std::regex)"p", u8"🅟");
		text = std::regex_replace(text, (std::regex)"q", u8"🅠");
		text = std::regex_replace(text, (std::regex)"r", u8"🅡");
		text = std::regex_replace(text, (std::regex)"s", u8"🅢");
		text = std::regex_replace(text, (std::regex)"t", u8"🅣");
		text = std::regex_replace(text, (std::regex)"u", u8"🅤");
		text = std::regex_replace(text, (std::regex)"v", u8"🅥");
		text = std::regex_replace(text, (std::regex)"w", u8"🅦");
		text = std::regex_replace(text, (std::regex)"x", u8"🅧");
		text = std::regex_replace(text, (std::regex)"y", u8"🅨");
		text = std::regex_replace(text, (std::regex)"z", u8"🅩");

		text = std::regex_replace(text, (std::regex)"0", u8"⓿");
		text = std::regex_replace(text, (std::regex)"1", u8"➊");
		text = std::regex_replace(text, (std::regex)"2", u8"➋");
		text = std::regex_replace(text, (std::regex)"3", u8"➌");
		text = std::regex_replace(text, (std::regex)"4", u8"➍");
		text = std::regex_replace(text, (std::regex)"5", u8"➎");
		text = std::regex_replace(text, (std::regex)"6", u8"➏");
		text = std::regex_replace(text, (std::regex)"7", u8"➐");
		text = std::regex_replace(text, (std::regex)"8", u8"➑");
		text = std::regex_replace(text, (std::regex)"9", u8"➒");
		return text;
	};
	static std::string Bold(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"ᴀ");
		text = std::regex_replace(text, (std::regex)"B", u8"ʙ");
		text = std::regex_replace(text, (std::regex)"C", u8"ᴄ");
		text = std::regex_replace(text, (std::regex)"D", u8"ᴅ");
		text = std::regex_replace(text, (std::regex)"E", u8"ᴇ");
		text = std::regex_replace(text, (std::regex)"F", u8"ꜰ");
		text = std::regex_replace(text, (std::regex)"G", u8"ɢ");
		text = std::regex_replace(text, (std::regex)"H", u8"ʜ");
		text = std::regex_replace(text, (std::regex)"I", u8"ɪ");
		text = std::regex_replace(text, (std::regex)"J", u8"ᴊ");
		text = std::regex_replace(text, (std::regex)"K", u8"ᴋ");
		text = std::regex_replace(text, (std::regex)"L", u8"ʟ");
		text = std::regex_replace(text, (std::regex)"M", u8"ᴍ");
		text = std::regex_replace(text, (std::regex)"N", u8"ɴ");
		text = std::regex_replace(text, (std::regex)"O", u8"ᴏ");
		text = std::regex_replace(text, (std::regex)"P", u8"ᴘ");
		text = std::regex_replace(text, (std::regex)"Q", u8"ǫ");
		text = std::regex_replace(text, (std::regex)"R", u8"ʀ");
		text = std::regex_replace(text, (std::regex)"S", u8"ꜱ");
		text = std::regex_replace(text, (std::regex)"T", u8"ᴛ");
		text = std::regex_replace(text, (std::regex)"U", u8"ᴜ");
		text = std::regex_replace(text, (std::regex)"V", u8"ᴠ");
		text = std::regex_replace(text, (std::regex)"W", u8"ᴡ");
		text = std::regex_replace(text, (std::regex)"X", u8"x");
		text = std::regex_replace(text, (std::regex)"Y", u8"ʏ");
		text = std::regex_replace(text, (std::regex)"Z", u8"ᴢ");

		text = std::regex_replace(text, (std::regex)"a", u8"ᴀ");
		text = std::regex_replace(text, (std::regex)"b", u8"ʙ");
		text = std::regex_replace(text, (std::regex)"c", u8"ᴄ");
		text = std::regex_replace(text, (std::regex)"d", u8"ᴅ");
		text = std::regex_replace(text, (std::regex)"e", u8"ᴇ");
		text = std::regex_replace(text, (std::regex)"f", u8"ꜰ");
		text = std::regex_replace(text, (std::regex)"g", u8"ɢ");
		text = std::regex_replace(text, (std::regex)"h", u8"ʜ");
		text = std::regex_replace(text, (std::regex)"i", u8"ɪ");
		text = std::regex_replace(text, (std::regex)"j", u8"ᴊ");
		text = std::regex_replace(text, (std::regex)"k", u8"ᴋ");
		text = std::regex_replace(text, (std::regex)"l", u8"ʟ");
		text = std::regex_replace(text, (std::regex)"m", u8"ᴍ");
		text = std::regex_replace(text, (std::regex)"n", u8"ɴ");
		text = std::regex_replace(text, (std::regex)"o", u8"ᴏ");
		text = std::regex_replace(text, (std::regex)"p", u8"ᴘ");
		text = std::regex_replace(text, (std::regex)"q", u8"ǫ");
		text = std::regex_replace(text, (std::regex)"r", u8"ʀ");
		text = std::regex_replace(text, (std::regex)"s", u8"ꜱ");
		text = std::regex_replace(text, (std::regex)"t", u8"ᴛ");
		text = std::regex_replace(text, (std::regex)"u", u8"ᴜ");
		text = std::regex_replace(text, (std::regex)"v", u8"ᴠ");
		text = std::regex_replace(text, (std::regex)"w", u8"ᴡ");
		text = std::regex_replace(text, (std::regex)"x", u8"x");
		text = std::regex_replace(text, (std::regex)"y", u8"ʏ");
		text = std::regex_replace(text, (std::regex)"z", u8"ᴢ");

		text = std::regex_replace(text, (std::regex)"1", u8"₁");
		text = std::regex_replace(text, (std::regex)"2", u8"₂");
		text = std::regex_replace(text, (std::regex)"3", u8"₃");
		text = std::regex_replace(text, (std::regex)"4", u8"₄");
		text = std::regex_replace(text, (std::regex)"5", u8"₅");
		text = std::regex_replace(text, (std::regex)"6", u8"₆");
		text = std::regex_replace(text, (std::regex)"7", u8"₇");
		text = std::regex_replace(text, (std::regex)"8", u8"₈");
		text = std::regex_replace(text, (std::regex)"9", u8"₉");
		text = std::regex_replace(text, (std::regex)"0", u8"₀");
		return text;
	};
	static std::string Bubble(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"ᗩ");
		text = std::regex_replace(text, (std::regex)"B", u8"ᗷ");
		text = std::regex_replace(text, (std::regex)"C", u8"ᑕ");
		text = std::regex_replace(text, (std::regex)"D", u8"ᗪ");
		text = std::regex_replace(text, (std::regex)"E", u8"E");
		text = std::regex_replace(text, (std::regex)"F", u8"ᖴ");
		text = std::regex_replace(text, (std::regex)"G", u8"G");
		text = std::regex_replace(text, (std::regex)"H", u8"ᕼ");
		text = std::regex_replace(text, (std::regex)"I", u8"I");
		text = std::regex_replace(text, (std::regex)"J", u8"ᒍ");
		text = std::regex_replace(text, (std::regex)"K", u8"K");
		text = std::regex_replace(text, (std::regex)"L", u8"ᒪ");
		text = std::regex_replace(text, (std::regex)"M", u8"ᗰ");
		text = std::regex_replace(text, (std::regex)"N", u8"ᑎ");
		text = std::regex_replace(text, (std::regex)"O", u8"O");
		text = std::regex_replace(text, (std::regex)"P", u8"ᑭ");
		text = std::regex_replace(text, (std::regex)"Q", u8"ᑫ");
		text = std::regex_replace(text, (std::regex)"R", u8"ᖇ");
		text = std::regex_replace(text, (std::regex)"S", u8"ᔕ");
		text = std::regex_replace(text, (std::regex)"T", u8"T");
		text = std::regex_replace(text, (std::regex)"U", u8"ᑌ");
		text = std::regex_replace(text, (std::regex)"V", u8"ᐯ");
		text = std::regex_replace(text, (std::regex)"W", u8"ᗯ");
		text = std::regex_replace(text, (std::regex)"X", u8"᙭");
		text = std::regex_replace(text, (std::regex)"Y", u8"Y");
		text = std::regex_replace(text, (std::regex)"Z", u8"ᘔ");

		text = std::regex_replace(text, (std::regex)"a", u8"ᗩ");
		text = std::regex_replace(text, (std::regex)"b", u8"ᗷ");
		text = std::regex_replace(text, (std::regex)"c", u8"ᑕ");
		text = std::regex_replace(text, (std::regex)"d", u8"ᗪ");
		text = std::regex_replace(text, (std::regex)"e", u8"E");
		text = std::regex_replace(text, (std::regex)"f", u8"ᖴ");
		text = std::regex_replace(text, (std::regex)"g", u8"G");
		text = std::regex_replace(text, (std::regex)"h", u8"ᕼ");
		text = std::regex_replace(text, (std::regex)"i", u8"I");
		text = std::regex_replace(text, (std::regex)"j", u8"ᒍ");
		text = std::regex_replace(text, (std::regex)"k", u8"K");
		text = std::regex_replace(text, (std::regex)"l", u8"ᒪ");
		text = std::regex_replace(text, (std::regex)"m", u8"ᗰ");
		text = std::regex_replace(text, (std::regex)"n", u8"ᑎ");
		text = std::regex_replace(text, (std::regex)"o", u8"O");
		text = std::regex_replace(text, (std::regex)"p", u8"ᑭ");
		text = std::regex_replace(text, (std::regex)"q", u8"ᑫ");
		text = std::regex_replace(text, (std::regex)"r", u8"ᖇ");
		text = std::regex_replace(text, (std::regex)"s", u8"ᔕ");
		text = std::regex_replace(text, (std::regex)"t", u8"T");
		text = std::regex_replace(text, (std::regex)"u", u8"ᑌ");
		text = std::regex_replace(text, (std::regex)"v", u8"ᐯ");
		text = std::regex_replace(text, (std::regex)"w", u8"ᗯ");
		text = std::regex_replace(text, (std::regex)"x", u8"᙭");
		text = std::regex_replace(text, (std::regex)"y", u8"Y");
		text = std::regex_replace(text, (std::regex)"z", u8"ᘔ");

		text = std::regex_replace(text, (std::regex)"1", u8"１");
		text = std::regex_replace(text, (std::regex)"2", u8"２");
		text = std::regex_replace(text, (std::regex)"3", u8"３");
		text = std::regex_replace(text, (std::regex)"4", u8"４");
		text = std::regex_replace(text, (std::regex)"5", u8"５");
		text = std::regex_replace(text, (std::regex)"6", u8"６");
		text = std::regex_replace(text, (std::regex)"7", u8"７");
		text = std::regex_replace(text, (std::regex)"8", u8"８");
		text = std::regex_replace(text, (std::regex)"9", u8"９");
		text = std::regex_replace(text, (std::regex)"0", u8"０");
		return text;
	};
	static std::string Funky(std::string text) {
		text = std::regex_replace(text, (std::regex)"A", u8"卂");
		text = std::regex_replace(text, (std::regex)"B", u8"乃");
		text = std::regex_replace(text, (std::regex)"C", u8"匚");
		text = std::regex_replace(text, (std::regex)"D", u8"ᗪ");
		text = std::regex_replace(text, (std::regex)"E", u8"乇");
		text = std::regex_replace(text, (std::regex)"F", u8"千");
		text = std::regex_replace(text, (std::regex)"G", u8"Ꮆ");
		text = std::regex_replace(text, (std::regex)"H", u8"卄");
		text = std::regex_replace(text, (std::regex)"I", u8"丨");
		text = std::regex_replace(text, (std::regex)"J", u8"ﾌ");
		text = std::regex_replace(text, (std::regex)"K", u8"Ҝ");
		text = std::regex_replace(text, (std::regex)"L", u8"ㄥ");
		text = std::regex_replace(text, (std::regex)"M", u8"爪");
		text = std::regex_replace(text, (std::regex)"N", u8"几");
		text = std::regex_replace(text, (std::regex)"O", u8"ㄖ");
		text = std::regex_replace(text, (std::regex)"P", u8"卩");
		text = std::regex_replace(text, (std::regex)"Q", u8"Ɋ");
		text = std::regex_replace(text, (std::regex)"R", u8"尺");
		text = std::regex_replace(text, (std::regex)"S", u8"丂");
		text = std::regex_replace(text, (std::regex)"T", u8"ㄒ");
		text = std::regex_replace(text, (std::regex)"U", u8"ㄩ");
		text = std::regex_replace(text, (std::regex)"V", u8"ᐯ");
		text = std::regex_replace(text, (std::regex)"W", u8"山");
		text = std::regex_replace(text, (std::regex)"X", u8"乂");
		text = std::regex_replace(text, (std::regex)"Y", u8"ㄚ");
		text = std::regex_replace(text, (std::regex)"Z", u8"乙");

		text = std::regex_replace(text, (std::regex)"a", u8"卂");
		text = std::regex_replace(text, (std::regex)"b", u8"乃");
		text = std::regex_replace(text, (std::regex)"c", u8"匚");
		text = std::regex_replace(text, (std::regex)"d", u8"ᗪ");
		text = std::regex_replace(text, (std::regex)"e", u8"乇");
		text = std::regex_replace(text, (std::regex)"f", u8"千");
		text = std::regex_replace(text, (std::regex)"g", u8"Ꮆ");
		text = std::regex_replace(text, (std::regex)"h", u8"卄");
		text = std::regex_replace(text, (std::regex)"i", u8"丨");
		text = std::regex_replace(text, (std::regex)"j", u8"ﾌ");
		text = std::regex_replace(text, (std::regex)"k", u8"Ҝ");
		text = std::regex_replace(text, (std::regex)"l", u8"ㄥ");
		text = std::regex_replace(text, (std::regex)"m", u8"爪");
		text = std::regex_replace(text, (std::regex)"n", u8"几");
		text = std::regex_replace(text, (std::regex)"o", u8"ㄖ");
		text = std::regex_replace(text, (std::regex)"p", u8"卩");
		text = std::regex_replace(text, (std::regex)"q", u8"Ɋ");
		text = std::regex_replace(text, (std::regex)"r", u8"尺");
		text = std::regex_replace(text, (std::regex)"s", u8"丂");
		text = std::regex_replace(text, (std::regex)"t", u8"ㄒ");
		text = std::regex_replace(text, (std::regex)"u", u8"ㄩ");
		text = std::regex_replace(text, (std::regex)"v", u8"ᐯ");
		text = std::regex_replace(text, (std::regex)"w", u8"山");
		text = std::regex_replace(text, (std::regex)"x", u8"乂");
		text = std::regex_replace(text, (std::regex)"y", u8"ㄚ");
		text = std::regex_replace(text, (std::regex)"z", u8"乙");

		text = std::regex_replace(text, (std::regex)"1", u8"1");
		text = std::regex_replace(text, (std::regex)"2", u8"2");
		text = std::regex_replace(text, (std::regex)"3", u8"3");
		text = std::regex_replace(text, (std::regex)"4", u8"4");
		text = std::regex_replace(text, (std::regex)"5", u8"5");
		text = std::regex_replace(text, (std::regex)"6", u8"6");
		text = std::regex_replace(text, (std::regex)"7", u8"7");
		text = std::regex_replace(text, (std::regex)"8", u8"8");
		text = std::regex_replace(text, (std::regex)"9", u8"9");
		text = std::regex_replace(text, (std::regex)"0", u8"0");
		return text;
	};
	static std::string integer(std::string text) {
		text = std::regex_replace(text, (std::regex)"i", u8"1");
		text = std::regex_replace(text, (std::regex)"t", u8"7");
		text = std::regex_replace(text, (std::regex)"e", u8"3");
		text = std::regex_replace(text, (std::regex)"a", u8"4");
		text = std::regex_replace(text, (std::regex)"g", u8"6");
		text = std::regex_replace(text, (std::regex)"s", u8"$");
		text = std::regex_replace(text, (std::regex)"o", u8"0");
		text = std::regex_replace(text, (std::regex)"l", u8"1");
		text = std::regex_replace(text, (std::regex)"I", u8"1");
		text = std::regex_replace(text, (std::regex)"T", u8"7");
		text = std::regex_replace(text, (std::regex)"E", u8"3");
		text = std::regex_replace(text, (std::regex)"A", u8"4");
		text = std::regex_replace(text, (std::regex)"G", u8"6");
		text = std::regex_replace(text, (std::regex)"S", u8"$");
		text = std::regex_replace(text, (std::regex)"O", u8"0");
		text = std::regex_replace(text, (std::regex)"L", u8"1");
		return text;
	};
};